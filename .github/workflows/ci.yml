name: CI - Lint + Tests

on:
  push:
    branches: [ week12-portfolio-finance-capstone, main ]
  pull_request:
    branches: [ week12-portfolio-finance-capstone, main ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi
          pip install pytest pytest-cov flake8 black isort

      - name: Lint with flake8
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics || true

      - name: Check formatting with black
        run: black --check . || true

      - name: Check import order with isort
        run: isort --check-only . || true

      - name: Run pytest
        run: |
          pytest tests/ --cov=src --cov=medical_warehouse_dbt --cov-report=xml --cov-report=term-missing || true

      # Optional – only enable when you have CODECOV_TOKEN secret configured
      # - name: Upload coverage to Codecov
      #   uses: codecov/codecov-action@v3
      #   with:
      #     token: ${{ secrets.CODECOV_TOKEN }}
      #     fail_ci_if_error: false